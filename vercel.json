{
  "buildCommand": "apt-get update && apt-get install -y python3 python3-pip ffmpeg && pip3 install yt-dlp && npm run build",
  "functions": {
    "app/api/info/route.ts": {
      "maxDuration": 60
    },
    "app/api/search/route.ts": {
      "maxDuration": 60
    },
    "app/api/stream/route.ts": {
      "maxDuration": 300
    }
  }
}
